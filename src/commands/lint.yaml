# src/commands/lint.yaml
---
description: Run Hadolint against Dockerfile.

parameters:

  dockerfile:
    description: Name of dockerfile
    type: string
    default: Dockerfile

  additional-commands:
    description: hadolint flags
    type: string
    default: ""

steps:
  - run:
      name: hadolint Dockerfile
      command: hadolint << parameters.additional-commands >> << parameters.dockerfile >>
